import forEach from 'lodash-es/forEach';

var propsMap = {};

export default function addProvidedProps(name, props) {
  propsMap[name] = props;
}

export var getProvidedProps = function getProvidedProps() {
  var combined = [];
  forEach(propsMap, function (props) {
    Array.prototype.push.apply(combined, props);
  });

  return combined;
};